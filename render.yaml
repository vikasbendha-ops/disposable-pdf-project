services:
  - type: web
    name: disposable-pdf-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements-runtime.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
